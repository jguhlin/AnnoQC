pfam_db = "share/pfam/Pfam-A.hmm"
pfam_clans = "share/pfam/Pfam-A.clans.tsv"

[scoring.weights]
homology = 0.5
intrinsic = 0.3
taxonomy = 0.0
domains = 0.1
length = 0.1

[consensus]
min_hits = 5
max_panel = 20

[taxonomy]
enabled = false

# Top-level Pfam paths for HMMER (must be at root, not under [taxonomy])
pfam_db = "share/pfam/Pfam-A.hmm"
pfam_clans = "share/pfam/Pfam-A.clans.tsv"
